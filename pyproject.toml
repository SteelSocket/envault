[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "envault"
version = "0.1.0"
description = "Encrypted Vault"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
envault = "envault.__main__:main"
